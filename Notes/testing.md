# Отчёт о покрытии тестами (Backend)

**Дата проведения замеров:** 28 августа 2026 г.  
**Стек:** Django REST Framework, pytest, pytest-cov, freezegun, Docker Compose

---

## 1. Основные метрики

| Метрика                                     | Значение                                                                     |
|:--------------------------------------------|:-----------------------------------------------------------------------------|
| **Общее количество тестов**                 | 37                                                                           |
| **Итоговое покрытие (Coverage)**            | **~90–95%** (с исключенными служебными модулями)                             |
| **Время первого (холодного) прогона**       | ~3.8 сек                                                                     |
| **Время повторного прогона (`--reuse-db`)** | ~1.2 сек                                                                     |
| **Изоляция тестовой базы**                  | База `test_postgres_db` создаётся автоматически, dev-данные не затрагиваются |

---

## 2. Команды запуска (`Makefile`)

Все тесты запускаются внутри контейнера Docker одной командой с хост-машины:

* **`make test`** — запуск всех тестов с терминальным отчётом о непокрытых строках (`--cov-report=term-missing`).
* **`make test-html`** — генерация визуального HTML-отчёта в папке `htmlcov/` (`start htmlcov\index.html` на Windows).
* **`make test-fast`** — быстрая проверка тестов с переиспользованием базы данных (`--reuse-db`).

---

## 3. Настройка исключений (`.coveragerc`)

Из расчёта покрытия исключены служебные файлы, точки входа и генерации кода:

* Миграции базы данных (`*/migrations/*`)
* Конфигурации и WSGI/ASGI (`config/settings.py`, `config/wsgi.py`, `config/asgi.py`)
* Управляющие и демонстрационные скрипты (`manage.py`, `orm_examples.py`)
* Сами файлы тестов (`*/tests/*`, `*_tests.py`, `tests.py`)

---

## 4. Анализ непокрытых участков и решения

1. **`tasks/viewsets.py` (метод `stats`):**
    * *Решение:* Покрыт отдельным интеграционным тестом эндпоинта `/api/tasks/stats/` (проверка ORM и RAW SQL).
2. **`tasks/services.py` (внешние уведомления):**
    * *Решение:* Замокано через `@patch.object(TaskViewSet, "send_task_created_notification")`. Протестированы сценарии
      успеха и сетевой ошибки.
3. **`config/limiterConfig.py`:**
    * *Решение:* Инфраструктурный конфигуратор Rate Limiting, вынесен из обязательного unit-покрытия.

---

## 5. Выполнение критериев приёмки

- [x] `pytest --cov` работает корректно, HTML-отчёт генерируется в `htmlcov/`.
- [x] Миграции и системные конфиги исключены из отчёта.
- [x] `make test` выложен в `Makefile`, запускает тесты в Docker и проходит с зелёным статусом.
- [x] Тесты в контейнере работают в изолированной `test_*` БД и не затрагивают рабочую базу данных.
- [x] Цифры покрытия и метрики зафиксированы в `notes/testing.md`.

# Итоговый отчёт по тестированию (W9)

**Дата замеров:** 28 августа 2026 г.  
**Стек:** Django REST Framework, pytest, freezegun | Vue 3, Vitest, Vue Test Utils, Pinia

---

## 1. Инструменты и запуск

| Среда        | Технологии                                   | Команда в Docker                            | Команда локально |
|:-------------|:---------------------------------------------|:--------------------------------------------|:-----------------|
| **Backend**  | pytest, pytest-django, pytest-cov, freezegun | `make test`                                 | `pytest --cov=.` |
| **Frontend** | Vitest, @vue/test-utils, Pinia               | `docker compose exec frontend npm run test` | `npm run test`   |

---

## 2. Соглашения (Conventions)

### Backend

* **Расположение:** Папка `tests/` в корне проекта или директории приложений (`tasks/tests/`).
* **Именование файлов:** `test_*.py` (например, `test_timestamps.py`, `test_views.py`).
* **Именование функций:** `test_<модуль>_<сценарий>_<ожидаемый_результат>`.
* **Фикстуры:** Общие клиенты и авторизация вынесены в `tests/conftest.py`.

### Frontend

* **Расположение:** Папки `__tests__/` рядом с тестируемыми файлами или со стандартными расширениями.
* **Именование файлов:** `<Name>.spec.ts` или `<Name>.test.ts`.
* **Структура тестов:** Блоки `describe('Компонент/Store')` и `it('ожидаемое поведение')`.

---

## 3. Область покрытия по слоям

| Слой                    | Инструмент             | Что проверяем                                                   |
|:------------------------|:-----------------------|:----------------------------------------------------------------|
| **API-эндпоинты**       | `pytest` + `APIClient` | HTTP-статусы, структура JSON, состояние БД, права доступа       |
| **Модели и валидаторы** | `pytest` (unit)        | Бизнес-логика в изоляции, метод `__str__`, кастомная валидация  |
| **Pinia Stores**        | `Vitest` + API Mocks   | Смена `state` при вызове `actions`, обработка ошибок (4xx/5xx)  |
| **Composables**         | `Vitest`               | Изолированная реактивная логика и валидация форм                |
| **Vue Компоненты**      | `Vitest` + `VTU`       | Рендер по `props`, генерация событий (`emit`), `v-if` / `v-for` |

---

## 4. Что осознанно НЕ покрываем

* **CSS и верстка:** Отступы, цвета и адаптивность (проверяются визуально).
* **Коробочный код:** Внутренности Django, DRF, Pinia и UI-библиотек.
* **E2E-сценарии:** Полноценное сквозное тестирование браузера (вынесено в будущие спринты).
* **Админка Django:** Стандартный функционал `admin.py`.
* **Конфиги:** `settings.py`, `wsgi.py`, `vite.config.ts`, `manage.py`.

---

## 5. Стратегия мокирования (Mocks)

* **Backend:**
    * **Время:** `@freeze_time` из `freezegun` для фиксации `created_at` / `updated_at`.
    * **Сеть/Email:** `unittest.mock.patch` для внешних сервисов и уведомлений.
* **Frontend:**
    * **HTTP:** Мокирование `axios` через `vi.spyOn` или `vi.mock('axios')`.
    * **Маршрутизация:** Использование фейкового `createRouter` для тестов навигации.

---

## 6. Метрики покрытия (Конец W9)

* **Backend Coverage:** **92%** (Миграции и конфиги исключены через `.coveragerc`)
* **Frontend Coverage:** **88%** (Компоненты, composables и stores)

---

## 7. Технический долг

1. **[High]** Покрыть граничные случаи Rate Limiting (429 Too Many Requests) на бэкенде.
2. **[Medium]** Написать интеграционный тест на сброс пароля.
3. **[Low]** Настроить сквозной E2E-тест авторизации через Playwright.